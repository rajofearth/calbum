#ifndef CALBUM_IMAGE_H
#define CALBUM_IMAGE_H

// image.h — Image loading library for calbum
// All types and function declarations are in src/types.h (app manifest).
// This header documents the public API for documentation/clarity only.
// Include src/types.h for actual compilation.

// Function declarations (documentation):
// int   il_init_wic(void);
// void  il_shutdown_wic(void);
// void *il_load_and_compress(const wchar_t *path, int thumb_size, int *out_size);
// void  il_free_bc1_data(void *data);
// int   il_get_image_dimensions(const wchar_t *path, int *out_w, int *out_h);
// void *il_load_full_image(const wchar_t *path, int *out_w, int *out_h);

#endif
