#ifndef CALBUM_FS_H
#define CALBUM_FS_H

// fs.h — File system library for calbum
// All types and function declarations are in src/types.h (app manifest).
// This header documents the public API for documentation/clarity only.
// Include src/types.h for actual compilation.

// Function declarations (documentation):
// int   fs_scan_directory(const wchar_t *dir, int *out_count);
// int   fs_has_image_extension(const wchar_t *name);
// DWORD fs_scan_thread(void *arg);
// int   fm_start_monitor(AppState *s, const wchar_t *dir);
// void  fm_stop_monitor(AppState *s);

#endif
