#ifndef CALBUM_GPU_H
#define CALBUM_GPU_H

// gpu.h — GPU rendering library for calbum
// Provides Direct3D 11 / Direct2D / DirectWrite rendering pipeline.
// All types and function declarations are in src/types.h.
//
// Sub-modules:
//   shader.c    — Embedded HLSL shader + compilation
//   device.c    — Device init/shutdown, resize, clear, present, blur
//   texture.c   — GPU texture pool management, instanced drawing
//   d2d.c       — D2D/DWrite text layout and rendering
//   fullimage.c — Full-resolution image GPU cache management

#endif
